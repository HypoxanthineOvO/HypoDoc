[project]
name = "hypolatex"
version = "0.3.0"
description = "HypoDoc LaTeX/PDF renderer and CLI"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0,<7.0",
    "typer>=0.12,<1.0",
]

[project.scripts]
hypolatex = "hypolatex.cli:app"

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.uv]
default-groups = []

[tool.hatch.build.targets.wheel]
packages = ["src/hypolatex"]

[tool.hatch.build.targets.wheel.force-include]
"tex/latex/hypolatex" = "hypolatex/tex/latex/hypolatex"
"themes/plain" = "hypolatex/themes/plain"

[tool.hatch.build.targets.sdist]
include = [
    "/src",
    "/tex",
    "/themes",
    "/README.md",
    "/LICENSE",
    "/CHANGELOG.md",
    "/docs",
    "/pyproject.toml",
]
exclude = [
    "/tests/private/**",
    "/tests/private_corpus.toml",
    "/build",
    "/dist",
    "/.pytest_cache",
    "/**/__pycache__",
    "/**/*.pyc",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
testpaths = ["tests/unit"]
