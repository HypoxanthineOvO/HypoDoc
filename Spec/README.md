# Spec

This directory is reserved for the independently versioned HypoDoc Spec at a
pinned compatible revision. It must remain the semantic authority for DSL
syntax, compatibility fixtures, and renderer obligations.

No specification source has been migrated yet.
